# Dataset Processors

Scripts for converting NBA data into E2E-Spot training format.

**Basketball processor:** `prepare_for_training.py` (in root directory)
- Converts NBA foul annotations → train/val/test JSON files
- Already run - cleaned dataset in `data/basketball/`
